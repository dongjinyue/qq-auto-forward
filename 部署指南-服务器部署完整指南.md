# QQ 消息推送机器人 - 服务器部署完整指南

> 本文档记录了从零到生产环境的完整部署流程，包括所有踩过的坑和解决方案。
> 目标：在云服务器上实现 QQ 消息 24 小时自动转发到企业微信「消息推送」群。

---

## 架构概览

```
QQ 客户端  ←→  NapCat (QQ协议端)  →→  中转服务 (Python)  →→  企业微信消息推送
                        ↓                     ↑
                   OneBot11 HTTP         HTTP POST 事件上报
                   Server (3001)          (3000/onebot/event)
```

**端口分配**：
| 端口 | 服务 | 说明 |
|------|------|------|
| 3001 | NapCat OneBot11 HTTP Server | 对外暴露 OneBot API |
| 3000 | 中转服务 (qq-forward) | 接收 NapCat 事件上报 |
| 6099 | NapCat WebUI | 管理界面（配置已固定 Token） |

---

## 一、云服务器准备

### 1.1 推荐配置

- **操作系统**：Ubuntu 24.04 LTS
- **规格**：2核 4G 轻量应用服务器
- **公网 IP**：记录下来，后续所有操作需要

### 1.2 安全组配置

在阿里云控制台 → 实例详情 → 安全组，添加以下规则：

| 协议 | 端口 | 来源 | 说明 |
|------|------|------|------|
| TCP | 22 | 0.0.0.0/0 | SSH 远程登录 |
| TCP | 6099 | 0.0.0.0/0 | NapCat WebUI 管理 |
| TCP | 3000 | 0.0.0.0/0 | 中转服务（可选，仅用于健康检查） |

> ⚠️ **注意**：NapCat 的 OneBot11 端口（3001）不需要对公网开放，仅本地回环即可。

### 1.3 服务器初始化

SSH 登录服务器后执行：

```bash
# 1. 更新系统
apt update && apt -y upgrade

# 2. 安装必要依赖
apt install -y git curl wget vim unzip build-essential screen xvfb

# 3. 安装 Node.js 18（使用 nvm 管理，避免系统包版本过旧）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18

# 4. 安装 PM2（进程守护 + 开机自启）
npm install -g pm2

# 5. 安装 Python 3 及 venv
apt install -y python3 python3-pip python3-venv
```

---

## 二、部署中转服务（Python）

### 2.1 上传代码

在本地 PowerShell 执行（将 IP 替换为你的服务器公网 IP）：

```powershell
# 打包项目（排除虚拟环境和缓存）
cd 项目目录
tar czf qq-forward.tar.gz --exclude=venv --exclude=__pycache__ --exclude=*.pyc .

# 上传到服务器
scp qq-forward.tar.gz root@你的服务器IP:/opt/
```

服务器上解压并安装依赖：

```bash
cd /opt
mkdir -p qq-forward
tar xzf qq-forward.tar.gz -C /opt/qq-forward
cd /opt/qq-forward

# 创建虚拟环境并安装依赖
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2.2 配置 config.py

打开 `/opt/qq-forward/config.py`，修改以下关键配置：

```python
# NapCat OneBot11 HTTP API 地址（重要！不是 3000 而是 3001）
NAPCAT_API_URL = "http://127.0.0.1:3001"

# NapCat OneBot11 Token（必须和 NapCat 配置一致）
NAPCAT_TOKEN = "your-napcat-token-here"

# 企业微信 Webhook URL
WECHAT_WEBHOOK_URL = "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=你的KEY"

# QQ 群白名单
TARGET_GROUP_IDS = [1063720812, 467810240]

# 调试开关
DEBUG = True
```

> ⚠️ **踩坑记录**：`NAPCAT_API_URL` 很多人会写成 3000，但正确值是 **3001**！3000 是中转服务自己的端口，3001 才是 NapCat OneBot11 的 HTTP API 端口。

### 2.3 启动中转服务

```bash
cd /opt/qq-forward
source venv/bin/activate

# 测试启动（前台运行，验证无误后再用 PM2 守护）
uvicorn main:app --host 127.0.0.1 --port 3000

# 验证健康检查（另开终端）
curl http://127.0.0.1:3000/health
# 预期返回：{"status":"ok","webhook_configured":true,...}

# 测试无误后，用 PM2 守护
pm2 start "venv/bin/uvicorn main:app --host 127.0.0.1 --port 3000" --name qq-forward
pm2 save
pm2 startup systemd
# 执行上面命令输出的那行命令（自动配置开机自启）
```

---

## 三、部署 NapCat（QQ 协议端）

### 3.1 安装 Linux QQ + NapCat

**不要使用 NapCat Shell 版**（Windows 版在 Linux 上存在大量兼容性问题）。正确方式是使用官方一键安装脚本：

```bash
# 下载 NapCat 官方安装脚本
wget https://raw.githubusercontent.com/NapNeko/NapCatQQ/main/install/install-linux.sh
chmod +x install-linux.sh

# 执行安装
./install-linux.sh

# 或者直接使用官方一键命令（推荐）
bash <(curl -sL https://napneko.github.io/install-linux.sh)
```

安装完成后，NapCat 会安装在 `/opt` 目录下。

### 3.2 配置 NapCat

#### 3.2.1 修改 OneBot11 配置

找到 OneBot11 配置文件（通常路径为 `/opt/napcat/config/onebot11_<QQ号>.json`）：

```bash
# 查看配置文件
ls /opt/napcat/config/onebot11_*.json

# 编辑配置（替换为你的 QQ 号）
nano /opt/napcat/config/onebot11_你的QQ号.json
```

配置内容应为：

```json
{
  "network": {
    "httpServers": [
      {
        "name": "qq-forward-api",
        "enable": true,
        "host": "0.0.0.0",
        "port": 3001,
        "token": "your-napcat-token-here",
        "enableCors": true,
        "eventEnable": true,
        "eventPostFormat": "array"
      }
    ],
    "httpClients": [
      {
        "name": "qq-forward-webhook",
        "enable": true,
        "url": "http://127.0.0.1:3000/onebot/event",
        "token": "your-napcat-token-here",
        "secret": "",
        "groupEnable": true,
        "privateEnable": true,
        "noticeEnable": true,
        "requestEnable": true,
        "postFormat": "array"
      }
    ]
  }
}
```

**关键字段说明**：
- `enable: true` - **必须显式启用**，否则 OneBot11 不会加载
- `httpServers[0].port: 3001` - OneBot11 HTTP Server 端口
- `httpClients[0].url` - 事件上报到中转服务的地址
- `httpClients[0].token` - 和 `config.py` 中的 `NAPCAT_TOKEN` 保持一致

> ⚠️ **踩坑记录**：配置文件里的 `enable` 字段默认可能是 `false`，如果不改，OneBot11 不会启动，3001 端口永远不会监听！

#### 3.2.2 修改 NapCat 主配置

编辑 `/opt/napcat/config/napcat_<QQ号>.json`，确保日志功能开启：

```json
{
  "fileLog": true,
  "consoleLog": true,
  "fileLogLevel": "debug",
  "consoleLogLevel": "info"
}
```

#### 3.2.3 修改 WebUI 配置

编辑 `/opt/napcat/config/webui.json`，**固定 Token 并绑定 IPv4**：

```bash
python3 << 'EOF'
import json

with open('/opt/napcat/config/webui.json', 'r') as f:
    config = json.load(f)

# 固定 Token（不要每次重启都变）
config['token'] = 'your-webui-token-here'
# 绑定 IPv4（默认是 :: IPv6，公网访问会失败）
config['host'] = '0.0.0.0'
config['enable2FA'] = False

with open('/opt/napcat/config/webui.json', 'w') as f:
    json.dump(config, f, indent=4, ensure_ascii=False)

print("WebUI 配置已修复")
EOF
```

修复后，WebUI 地址固定为：`http://你的服务器IP:6099/webui?token=your-webui-token-here`

### 3.3 启动 NapCat（带自动登录）

创建 PM2 启动脚本：

```bash
cat > /opt/pm2_napcat.sh << 'EOF'
#!/bin/bash
export HOME=/root
export DISPLAY=:1
export LD_PRELOAD=/opt/libnapcat_launcher.so
cd /opt

# 清理旧的 Xvfb 进程
pkill -9 -f "Xvfb :1" 2>/dev/null
sleep 1

# 启动虚拟显示（Linux 无头运行需要）
Xvfb :1 -screen 0 1x1x8 +extension GLX +render > /tmp/xvfb_napcat.log 2>&1 &
sleep 1

# 启动 QQ 并指定 QQ 号自动登录
# ⚠️ -q 参数是关键！没有它每次都会要求扫码
exec /opt/QQ/qq --no-sandbox -q 你的QQ号
EOF

chmod +x /opt/pm2_napcat.sh
```

> ⚠️ **踩坑记录**：`-q 你的QQ号` 参数至关重要！没有这个参数，NapCat 会使用二维码登录而不是自动登录，每次重启都要扫码。

启动 NapCat：

```bash
# 先用 PM2 启动
export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
nvm use 18
pm2 start /opt/pm2_napcat.sh --name napcat
pm2 save
```

### 3.4 首次扫码登录

启动后 NapCat 会输出二维码，有两种方式扫码：

**方式 A：命令行扫码（首次使用）**
```bash
# 查看日志获取二维码
pm2 logs napcat --lines 30
```
复制日志中的 `二维码解码URL` 到浏览器打开，用手机 QQ 扫码。

**方式 B：WebUI 扫码（推荐，固定 Token 后使用）**
```
http://你的服务器IP:6099/webui?token=your-webui-token-here
```
在 WebUI 页面上扫码登录。

登录成功后，NapCat 会自动记住登录态，下次启动会自动恢复。

---

## 四、验证完整链路

### 4.1 检查服务状态

```bash
# 检查 PM2 进程
export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
nvm use 18
pm2 list
# 预期：napcat 和 qq-forward 都是 online

# 检查端口监听
ss -tlnp | grep -E ":(3000|3001|6099)"
# 预期：3000、3001、6099 全部在监听

# 检查 QQ 登录状态
curl -s -H "Authorization: Bearer your-napcat-token-here" http://127.0.0.1:3001/get_login_info
# 预期：返回 {"user_id":你的QQ号,"nickname":"你的昵称"}
```

### 4.2 发送测试消息

用另一个 QQ 号，在你配置的群（如 1063720812）里发一条文本消息。

查看中转服务日志：
```bash
pm2 logs qq-forward --lines 20
```

预期看到：
```
[HH:MM:SS] 📥 收到事件 post_type=message type=group
[HH:MM:SS] ✅ 文本转发成功 [【群名】昵称：消息内容...]
```

企业微信「消息推送」群应收到这条消息。

---

## 五、日常运维速查

### 5.1 查看日志

```bash
# 实时查看中转服务日志
pm2 logs qq-forward --lines 50

# 实时查看 NapCat 日志
pm2 logs napcat --lines 30

# 查看最近错误
pm2 logs qq-forward --nostream --lines 100 | grep -i "error\|⚠️\|❌"
```

### 5.2 重启服务

```bash
# 重启单个服务
pm2 restart qq-forward
pm2 restart napcat

# 重启全部服务
pm2 restart all
```

### 5.3 NapCat 掉线重登

如果 NapCat 日志显示需要扫码（登录态过期）：

```bash
# 1. 访问 WebUI 扫码
http://你的服务器IP:6099/webui?token=your-webui-token-here

# 2. 如果 WebUI 也不行，命令行重登
pm2 stop napcat
# 前台启动获取二维码
export DISPLAY=:1
Xvfb :1 -screen 0 1x1x8 +extension GLX +render &
/opt/QQ/qq --no-sandbox -q 你的QQ号
# 扫码成功后 Ctrl+C
# 3. 重新用 PM2 守护
pm2 start /opt/pm2_napcat.sh --name napcat
pm2 save
```

### 5.4 更新代码

```bash
# 拉取最新代码
cd /opt/qq-forward
git pull

# 重启中转服务
pm2 restart qq-forward
```

---

## 六、踩坑记录与解决方案

### 坑 1：NapCat Shell 版在 Linux 上不兼容

**问题**：下载 Windows 版 `NapCat.Shell.zip`，解压后引用 Windows 特有模块（如 `conout-wiJ7YKRd.js`），无法在 Linux 上运行。

**解决**：使用官方一键安装脚本 `install-linux.sh`，自动下载 Linux 版 QQ 和 NapCat Launcher。

---

### 坑 2：Docker Hub 拉取镜像超时

**问题**：尝试通过 Docker 容器化部署 NapCat，执行 `docker pull mlikiowa/napcat-docker` 时出现 `i/o timeout`。配置国内镜像源后仍不稳定。

**解决**：放弃 Docker 方案，改用官方 Linux 安装方案 + Xvfb 虚拟显示运行。

---

### 坑 3：dpkg 锁冲突

**问题**：执行 `apt install` 时提示 `E: Could not get lock /var/lib/dpkg/lock-frontend`，是阿里云自动更新进程占用了 apt 锁。

**解决**：编写等待循环：
```bash
while pgrep unattended-upgr; do sleep 5; done
```

---

### 坑 4：NapCat 重启后反复要求扫码

**问题**：每次 `pm2 restart napcat` 后，NapCat 都要求重新扫码，无法自动恢复登录态。

**根本原因**：启动脚本缺少 `-q QQ号` 参数，NapCat 默认使用二维码登录。

**解决**：在启动脚本中加上 `-q 你的QQ号` 参数：
```bash
exec /opt/QQ/qq --no-sandbox -q 你的QQ号
```

---

### 坑 5：OneBot11 的 3001 端口不监听

**问题**：NapCat 启动正常，但 `ss -tlnp | grep 3001` 查不到端口，OneBot API 调用失败。

**根本原因**：`onebot11_*.json` 配置文件中 `enable` 字段默认为 `false`，OneBot11 模块没有被加载。

**解决**：将 `enable` 显式设置为 `true`：
```json
{
  "network": {
    "httpServers": [{"enable": true, "port": 3001, ...}],
    "httpClients": [{"enable": true, "url": "...", ...}]
  }
}
```

---

### 坑 6：WebUI 公网无法访问

**问题**：本地服务器 `curl http://127.0.0.1:6099` 正常，但外部浏览器访问 `http://公网IP:6099` 超时。

**根本原因**：`webui.json` 中 `host` 字段默认为 `::`（IPv6），公网 IPv4 连接无法命中。

**解决**：修改为 `0.0.0.0` 同时绑定 IPv4 和 IPv6：
```python
config['host'] = '0.0.0.0'
```

---

### 坑 7：config.py 中端口和 Token 配置错误

**问题**：图片消息、合并转发消息全部失败。

**根本原因**：
- `NAPCAT_API_URL` 写成了 `http://127.0.0.1:3000`（中转服务自己的端口）
- `NAPCAT_TOKEN` 写成了占位符 `"请填入你的NapCat Token"`

**解决**：
```python
NAPCAT_API_URL = "http://127.0.0.1:3001"  # NapCat OneBot11 端口
NAPCAT_TOKEN = "your-napcat-token-here"   # 和 NapCat 配置一致
```

---

### 坑 8：合并转发消息编码错误

**问题**：日志中反复出现 `⚠️ 获取聊天记录失败：'ascii' codec can't encode characters`。

**原因**：处理合并转发消息时，请求 NapCat 获取原消息内容，URL 中包含中文字符导致编码错误。

**影响**：合并转发消息无法转发，但普通文本消息不受影响。

**后续修复方向**：需要在代码中统一使用 UTF-8 编码处理 URL 请求。

---

## 七、安全组端口总结

| 端口 | 服务 | 是否需要公网访问 |
|------|------|------------------|
| 22 | SSH | ✅ 是（远程登录） |
| 6099 | NapCat WebUI | ✅ 是（扫码登录、管理） |
| 3001 | OneBot11 HTTP Server | ❌ 否（仅本机回环） |
| 3000 | 中转服务 | 可选（健康检查用） |

**安全建议**：3000 端口不对公网开放，只在本机回环即可。

---

## 八、常见问题 FAQ

**Q: 服务器重启后服务会自动恢复吗？**
A: 会。PM2 已配置 `systemd` 开机自启，`pm2 save` 已保存进程列表。

**Q: QQ 登录态能保持多久？**
A: 一般 1-4 周。过期后需要在 WebUI 上重新扫码登录。

**Q: 如何修改转发的群？**
A: 修改 `/opt/qq-forward/config.py` 中的 `TARGET_GROUP_IDS`，然后 `pm2 restart qq-forward`。

**Q: 企业微信推送频率限制？**
A: 每个机器人每分钟最多 20 条消息。代码中已限制为 18 条/分钟。

**Q: 如何添加关键词过滤？**
A: 修改 `config.py` 中的 `KEYWORDS` 列表，只有包含任一关键词的消息才会转发。留空表示不过滤。
