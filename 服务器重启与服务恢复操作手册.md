# QQ 自动转发服务：重启与恢复操作手册

适用服务器：阿里云 Ubuntu 22.04，公网 IP `120.26.233.28`。

当前服务组成：

- `qq-forward`：接收 QQ 消息并转发到企业微信，监听 `127.0.0.1:3000`
- `napcat`：登录 QQ、接收消息并通过 OneBot 上报
- NapCat HTTP API：`127.0.0.1:3001`
- NapCat WebUI：服务器 `127.0.0.1:6099`，通过 SSH 隧道在本机访问
- 两个服务均由 PM2 守护，并由 `pm2-root.service` 开机自动恢复

> 安全提示：本文不保存服务器密码、企业微信 Webhook、OneBot Token 或 WebUI Token。不要把这些信息发到群聊或提交到公开仓库。

## 一、服务器重启后的正常恢复流程

### 第 1 步：启动阿里云服务器

如果服务器被关机，在阿里云 ECS 控制台找到实例，点击“启动”。等待约 1～2 分钟。

### 第 2 步：从本机连接服务器

在 Windows PowerShell 中执行：

```powershell
ssh root@120.26.233.28
```

按提示输入服务器密码。密码输入时屏幕不会显示字符，这是正常现象。

### 第 3 步：检查 PM2 和两个服务

登录服务器后执行：

```bash
systemctl status pm2-root --no-pager
/root/.nvm/versions/node/v18.20.8/bin/pm2 list
```

正常结果：

- `pm2-root.service` 显示 `active (running)`
- `qq-forward` 显示 `online`
- `napcat` 显示 `online`

如果两个进程都是 `online`，不要重复启动，直接执行下一步检查。

### 第 4 步：检查中转服务

```bash
curl http://127.0.0.1:3000/health
```

正常会返回包含以下内容的 JSON：

```json
{"status":"ok","service":"qq-forward","webhook_configured":true}
```

### 第 5 步：查看最近日志

```bash
/root/.nvm/versions/node/v18.20.8/bin/pm2 logs qq-forward --lines 50 --nostream
/root/.nvm/versions/node/v18.20.8/bin/pm2 logs napcat --lines 50 --nostream
```

NapCat 日志中出现 QQ 账号快速登录成功、OneBot HTTP 上报启动，说明 QQ 侧正常。

### 第 6 步：做一次真实转发测试

使用另一个 QQ，在目标 QQ 所在的群里发送一条纯文字测试消息。

不要使用运行 NapCat 的机器人 QQ 自己发消息；当前配置会忽略机器人自己的消息，以防止消息循环。

查看日志：

```bash
/root/.nvm/versions/node/v18.20.8/bin/pm2 logs qq-forward --lines 100
```

看到“收到事件”和“文本转发成功”，且企业微信群收到消息，即恢复完成。按 `Ctrl+C` 只会退出日志查看，不会停止服务。

## 二、服务被手动关闭后的快速启动

如果只是执行过 `pm2 stop`，运行：

```bash
/root/.nvm/versions/node/v18.20.8/bin/pm2 restart qq-forward
/root/.nvm/versions/node/v18.20.8/bin/pm2 restart napcat
/root/.nvm/versions/node/v18.20.8/bin/pm2 save
```

然后再次检查：

```bash
/root/.nvm/versions/node/v18.20.8/bin/pm2 list
curl http://127.0.0.1:3000/health
```

## 三、PM2 没有自动恢复时

先恢复 PM2 的开机服务和已保存进程：

```bash
systemctl restart pm2-root
/root/.nvm/versions/node/v18.20.8/bin/pm2 resurrect
/root/.nvm/versions/node/v18.20.8/bin/pm2 list
```

如果进程列表里仍然没有 `qq-forward`，执行：

```bash
cd /opt/qq-forward
/root/.nvm/versions/node/v18.20.8/bin/pm2 start "venv/bin/uvicorn main:app --host 127.0.0.1 --port 3000" --name qq-forward
```

如果进程列表里仍然没有 `napcat`，执行：

```bash
/root/.nvm/versions/node/v18.20.8/bin/pm2 start /opt/start-napcat.sh --name napcat
```

最后保存进程清单并确认开机自启：

```bash
/root/.nvm/versions/node/v18.20.8/bin/pm2 save
systemctl enable pm2-root
systemctl restart pm2-root
```

## 四、QQ 掉线，需要重新登录时

NapCat 当前已配置 QQ 快速登录，普通服务器重启通常不需要重新扫码。只有日志显示 QQ 离线、登录失效或需要验证时，才进入 WebUI。

### 第 1 步：建立 WebUI 安全隧道

在本机新开一个 PowerShell 窗口，执行：

```powershell
ssh -N -L 16099:127.0.0.1:6099 root@120.26.233.28
```

输入密码后，这个窗口会一直停住且没有输出，这是隧道正在工作。登录期间不要关闭它。

### 第 2 步：打开 WebUI

浏览器访问：

```text
http://127.0.0.1:16099/webui/
```

如果页面要求 Token，可在另一个 SSH 窗口中查找 WebUI 配置：

```bash
find /opt -maxdepth 3 -name webui.json -print
```

查看上一步找到的文件，例如：

```bash
cat /opt/config/webui.json
```

把配置中的 Token 输入 WebUI，然后按页面提示扫码或确认登录。不要将 Token 发给其他人。

### 第 3 步：登录后重启 NapCat 并检查

```bash
/root/.nvm/versions/node/v18.20.8/bin/pm2 restart napcat
/root/.nvm/versions/node/v18.20.8/bin/pm2 logs napcat --lines 100
```

确认登录成功后，按 `Ctrl+C` 退出日志查看。再用另一个 QQ 发送纯文字进行真实测试。

## 五、常见故障处理

### 1. PM2 显示 online，但企业微信收不到消息

按顺序检查：

```bash
curl http://127.0.0.1:3000/health
/root/.nvm/versions/node/v18.20.8/bin/pm2 logs napcat --lines 100 --nostream
/root/.nvm/versions/node/v18.20.8/bin/pm2 logs qq-forward --lines 100 --nostream
```

重点判断：

- `qq-forward` 日志完全没有“收到事件”：NapCat 的 OneBot HTTP 上报未启动或 QQ 已掉线
- 有“收到事件”，但提示被过滤：检查群白名单、关键词或是否由机器人自己发送
- 有“转发失败”：检查企业微信 Webhook 是否失效、服务器能否访问企业微信

当前 OneBot 上报地址应为：

```text
http://127.0.0.1:3000/onebot/event
```

### 2. NapCat 提示找不到 `/opt/napcat/napcat.mjs`

检查符号链接：

```bash
ls -l /opt/napcat/napcat.mjs
```

如果文件不存在，重新创建并只重启 NapCat：

```bash
ln -s /opt/napcat.mjs /opt/napcat/napcat.mjs
/root/.nvm/versions/node/v18.20.8/bin/pm2 restart napcat
```

### 3. 文字能转发，但图片不能转发

这和服务是否启动是两件事。当前 QQ/NapCat 版本可能出现图片下载超时或 QQ CDN 返回错误；文字链路仍可正常工作。先查看 NapCat 和 `qq-forward` 日志，不要反复重启或重装，以免破坏已经可用的登录和文字转发。

### 4. 修改代码或配置后如何生效

只重启中转服务：

```bash
/root/.nvm/versions/node/v18.20.8/bin/pm2 restart qq-forward
```

修改 OneBot/NapCat 配置后，只重启 NapCat：

```bash
/root/.nvm/versions/node/v18.20.8/bin/pm2 restart napcat
```

## 六、最短恢复命令（熟悉后使用）

SSH 登录服务器后依次执行：

```bash
systemctl restart pm2-root
/root/.nvm/versions/node/v18.20.8/bin/pm2 restart qq-forward
/root/.nvm/versions/node/v18.20.8/bin/pm2 restart napcat
/root/.nvm/versions/node/v18.20.8/bin/pm2 save
/root/.nvm/versions/node/v18.20.8/bin/pm2 list
curl http://127.0.0.1:3000/health
```

随后使用另一个 QQ 在群里发一条纯文字测试消息。企业微信群收到后即可继续 24 小时运行。
