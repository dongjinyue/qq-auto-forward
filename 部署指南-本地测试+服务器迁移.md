# 本地开发与服务器部署一条龙指南

本指南分三部分：
1. **本地跑通链路**（不用 NapCat，不用云服务器，先验证代码和企业微信推送）
2. **本地接入真实 NapCat + QQ**（把代码和真实QQ打通）
3. **迁移到云服务器**（确保24h运行）

---

## 一、本地测试：不跑 NapCat，先验证代码和企业微信

### 1.1 填配置

打开 `config.py`，做两处修改：

```python
# 粘贴你的企业微信消息推送Webhook完整URL
WECHAT_WEBHOOK_URL = "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=你的KEY贴这里"

# ② 关键词过滤先留空，白名单先留空，确保测试消息不会被意外过滤
TARGET_GROUP_IDS = []
KEYWORDS = []
```

先在 PowerShell 单独测试 Webhook（必做，出问题好排查）：

```powershell
curl -Method Post -Uri "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=你的KEY" -ContentType "application/json" -Body '{"msgtype":"text","text":{"content":"单独测试：Webhook可用！"}}'
```

企业微信群里能看到消息 → OK。

### 1.2 安装 Python 依赖

项目根目录（有 requirements.txt 那个目录）下，PowerShell 执行：

```powershell
cd 项目目录
python -m venv venv

# Windows venv 激活（PowerShell）：
venv\Scripts\Activate.ps1

# 如果提示策略限制，先执行：
#   Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

pip install -r requirements.txt
```

### 1.3 启动中转服务

**终端1**：
```powershell
# 确保 venv 已激活
venv\Scripts\Activate.ps1

uvicorn main:app --reload --host 127.0.0.1 --port 8080
```

看到类似如下输出就说明启动成功：
```
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8080 (Press CTRL+C to quit)
```

### 1.4 运行端到端测试脚本

**终端2**（别关终端1）：
```powershell
cd 项目目录
venv\Scripts\Activate.ps1
python test_local.py
```

预期：
- 脚本输出 `✅ 服务在线`、各条测试的响应
- 企业微信群里至少收到 **"普通文本消息"** 和 **"含@和图片的复合消息"** 两条
- 终端1（uvicorn）的日志有彩色 emoji，清晰显示每条消息的处理

如果没收到：
- 看终端1的日志，里面有详细说明被过滤的原因
- 常见原因：Webhook URL 没填、TARGET_GROUP_IDS 里没有 123456789、KEYWORDS 没清空

---

## 二、本地接入真实 NapCat + QQ 小号

上一步验证了 "中转服务 → 企业微信" 是通的。这一步把 "真实QQ → 中转服务" 也打通。

### 2.1 选 NapCat 版本

本地 Windows 推荐 **NapCat Desktop**（图形界面，内置浏览器、扫码方便）：
- 下载地址：去 `https://github.com/NapNeko/NapCatQQ/releases` 找 `NapCat.Desktop.zip`
- 下载解压，双击 `NapCat.exe` 打开

> 想命令行跑也可以选 `NapCat.Shell.zip`，但 Desktop 版扫码和配置更省心，本地首选。

### 2.2 登录小号

1. 打开 NapCat Desktop 后，会弹出 NTQQ 原生登录界面
2. 用 **QQ小号** 扫码登录（不要用主号！）
3. 登录成功后，左上角会显示账号信息

### 2.3 配置 OneBot11 HTTP 上报到中转服务

这一步是关键：让 NapCat 收到 QQ 消息后，POST 到我们的中转服务。

1. NapCat Desktop 左侧菜单 → **OneBot 11**
2. 选择你的账号 → 点击 **配置**（或「配置 HTTP 客户端」）
3. 核心配置项：
   - **HTTP 客户端上报地址**：`http://127.0.0.1:8080/onebot/event`
   - **上报格式**：array（数组格式）
   - **开启 HTTP 上报**：打开 / Enable
   - **上报自己的消息**：关闭 / 否（防止回环，config.py 里也有一层过滤）
4. 保存配置 → **重启 NapCat**（必要，否则配置不生效）

### 2.4 验证真实消息链路

1. 确保本地中转服务在跑（终端1的 uvicorn 没停）
2. 用 **另一个大号QQ**，在小号也在的某个群里，发一条纯文本，比如 "测试QQ转发"
3. 观察 uvicorn 终端的日志，应该出现：
   ```
   [HH:MM:SS] 📥 收到事件 post_type=message type=group
   [HH:MM:SS] ✅ 转发成功 [【群名】xxx：测试QQ转发...]
   ```
4. 企业微信群里收到这条消息 → 整条链路完全打通！

如果没收到：
- NapCat 配置页里点"测试上报"，看看能不能 ping 通中转服务
- 确认中转服务的 `TARGET_GROUP_IDS` 包含这个群号，或者是空数组（放行全部群）

### 2.5 （可选）白名单配置

为避免被无关群刷屏，建议把 `config.py` 的 `TARGET_GROUP_IDS` 填成你真正要转发的群：

```python
TARGET_GROUP_IDS = [
    123456789,   # 例如你的工作群
]
```

改完后 `Ctrl+C` 停 uvicorn，再重跑命令（因为开了 `--reload`，保存文件应该会自动重启，实际观察确认即可）。

---

## 三、迁移到云服务器（24小时运行）

本地全部跑通后，把代码和 NapCat 搬上云服务器。下面以 **Ubuntu 22.04 LTS** 为例。

### 3.1 服务器初始化（只做一次）

SSH 连上服务器后执行：

```bash
# 1. 更新并装基础包
apt update && apt -y upgrade
apt install -y git curl wget vim unzip build-essential screen

# 2. 装 Node.js 18（NapCat 需要）
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs
node -v   # 应该输出 v18.x.x

# 3. 装 Python 3.10+ 和 venv
apt install -y python3 python3-pip python3-venv
python3 --version

# 4. 装 pm2（进程守护、开机自启）
npm install -g pm2
```

云服务器控制台「安全组」放行：
- TCP 端口 `6099`：NapCat WebUI（可选，方便在线配置）
- 端口 `8080`：中转服务接收上报（其实不用对外，本机回环即可，但放行方便远程 health 检查）

> 如果只在本机回环（NapCat 和中转服务都在一台服务器上），8080 不需要放公网，更安全。

### 3.2 上传项目代码到服务器

方式A（推荐，用 scp）：在本地 PowerShell 执行（IP 换成你的公网IP）：

```powershell
# 打包（排除 venv、__pycache__ 等大目录）
cd 项目目录
tar czf qq-forward.tar.gz --exclude=venv --exclude=__pycache__ --exclude=*.pyc .

# 上传到服务器
scp qq-forward.tar.gz root@你的服务器IP:/opt/
```

然后回到服务器 SSH：

```bash
mkdir -p /opt/qq-forward
cd /opt
tar xzf qq-forward.tar.gz -C /opt/qq-forward
cd /opt/qq-forward

# 创建 venv 并安装依赖
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

方式B（会用 git 的话）：把代码推到 GitHub 仓库，服务器 `git clone`，以后更新更方便。

### 3.3 确认 config.py 在服务器上的值

服务器上的 `config.py`：
- `WECHAT_WEBHOOK_URL` 要和本地一样，保持正确
- `TARGET_GROUP_IDS` 填要转发的群
- `DEBUG` 可以改成 `False` 少打点日志

### 3.4 用 pm2 启动中转服务

```bash
cd /opt/qq-forward
pm2 start "venv/bin/uvicorn main:app --host 127.0.0.1 --port 8080" --name qq-forward
pm2 save
pm2 startup   # 执行输出的那行命令（自动设开机自启）

# 验证
pm2 list                 # qq-forward 应为 online
curl http://127.0.0.1:8080/health   # 应有 JSON 输出
pm2 logs qq-forward --lines 50      # 查看日志
```

### 3.5 服务器上部署 NapCat（QQ协议端）

推荐用 **NapCat Shell 版**（无头，适合 Linux 服务器）：

```bash
mkdir -p /opt/napcat && cd /opt/napcat

# 下载最新版（以官方 Release 实际文件名为准，先到 GitHub 确认版本号）
# 示例：假设最新版是 v1.x.x
wget https://github.com/NapNeko/NapCatQQ/releases/latest/download/NapCat.Shell.zip
unzip NapCat.Shell.zip

# 首次启动，会输出二维码
cd /opt/napcat
npm install  # 或官方指定命令，有的版本直接 npm run start
```

首次启动会在终端输出二维码，用 **QQ小号** 扫码登录。登录成功后 Ctrl+C 停掉。

**配置 NapCat 的 HTTP 上报：**

NapCat 的配置文件一般在 `data/napcat/config/onebot11_<QQ号>.json` 或 `config/onebot11.json`（路径以实际版本为准）。找到后改成：

```json
{
  "http": { "enable": false },
  "httpServer": [],
  "httpClient": {
    "enable": true,
    "url": "http://127.0.0.1:8080/onebot/event",
    "messagePostFormat": "array",
    "reportSelfMessage": false,
    "token": ""
  },
  "ws": { "enable": false },
  "wsServer": [],
  "debug": false,
  "heartInterval": 30000
}
```

**关键点**：`httpClient.url` 指向服务器本机的中转服务 `http://127.0.0.1:8080/onebot/event`

**用 pm2 守护 NapCat：**

```bash
cd /opt/napcat
pm2 start npm --name napcat -- run start    # 具体启动命令看官方文档
pm2 save
```

### 3.6 服务器端验证

```bash
pm2 list
# 应该看到 qq-forward 和 napcat 两个进程都是 online

# 日志
pm2 logs qq-forward
pm2 logs napcat
```

然后找一个小号所在的QQ群发消息，看 qq-forward 日志里出现 `✅ 转发成功`，企业微信群收到 → 完成。

### 3.7 日常运维命令速查

```bash
# 查看所有进程状态
pm2 list

# 看实时日志
pm2 logs                     # 全部
pm2 logs qq-forward --lines 200
pm2 logs napcat --lines 200

# 重启服务
pm2 restart qq-forward
pm2 restart napcat

# NapCat 掉线重登（最常见操作）
# 先停止 -> 前台启动一次重新扫码 -> 扫码成功后 Ctrl+C -> 再 pm2 启动
pm2 stop napcat
cd /opt/napcat
npm run start   # 会出二维码
# 扫码成功、看到登录成功的日志后 Ctrl+C
pm2 start napcat

# 看健康检查
curl http://127.0.0.1:8080/health
```

### 3.8 可选：加一个健康检查 crontab

```bash
cat > /opt/qq-forward/check.sh <<'EOF'
#!/bin/bash
if ! curl -sf http://127.0.0.1:8080/health > /dev/null; then
    pm2 restart qq-forward
    echo "$(date '+%F %T') qq-forward 已重启" >> /var/log/qq-forward-check.log
fi
EOF
chmod +x /opt/qq-forward/check.sh

(crontab -l 2>/dev/null; echo "*/5 * * * * /opt/qq-forward/check.sh") | crontab -
```

---

## 四、常见问题

**Q：NapCat 扫码后过几天掉线怎么办？**
A：按 3.7 节「NapCat 掉线重登」步骤操作即可。建议小号平时偶尔正常聊几句，降低风控概率。

**Q：中转服务更新了代码怎么热更新？**
A：
```bash
# 服务器上替换完新文件后
pm2 restart qq-forward
```
（如果是代码 git 仓库的话：`git pull && pm2 restart qq-forward`）

**Q：某个群消息太多刷屏？**
A：在 `config.py` 的 `KEYWORDS` 里加关键词，或把这个群号从 `TARGET_GROUP_IDS` 移除。
